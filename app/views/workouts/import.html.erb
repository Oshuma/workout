<h2>Import Workout</h2>

<%= form_tag(upload_workouts_path, multipart: true) do %>
  <div class="form-group">
    <%= file_field_tag(:csv_file, accept: '.csv, text/csv', class: 'form-control-file') %>
  </div>

  <%= submit_tag('Upload', class: 'btn btn-primary') %>
<% end %>
